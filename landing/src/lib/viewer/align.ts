// Compatibility re-export. Compare logic lives in `$lib/viewer/compare`.
export { alignBundles, compareRows, commonPeriods, sectionPresence } from './compare/rowCompare';
export type { AlignedRow } from './compare/types';
