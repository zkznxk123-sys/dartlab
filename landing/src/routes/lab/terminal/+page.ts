// /lab/terminal — DartLab Terminal (Bloomberg 스타일) 테스트 라우트.
// 정적 React+Babel 아티팩트(static/terminal/index.html)를 풀뷰포트 iframe 으로 격리 임베드.
// 검증 후 본진은 Svelte 포팅으로 승격(현재는 무재작성 검증 단계).
export const prerender = true;
export const ssr = false;
